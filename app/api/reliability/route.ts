import {NextResponse} from "next/server";
import {listReliability,prepareRecovery,recordFailure,resolveFailure,verifyRecovery} from "@/lib/reliability";
export async function GET(){return NextResponse.json(listReliability(),{headers:{"Cache-Control":"no-store"}})}
export async function POST(req:Request){try{const b=await req.json();if(b.action==="failure")return NextResponse.json({failure:recordFailure(b.value)},{status:201});if(b.action==="prepare")return NextResponse.json({plan:prepareRecovery(b.value)},{status:201});if(b.action==="resolve")return NextResponse.json({failure:resolveFailure(b.id,b.rootCause,b.regressionTestId)});if(b.action==="verify")return NextResponse.json({plan:verifyRecovery(b.id)});return NextResponse.json({error:"unknown action"},{status:400})}catch(e){return NextResponse.json({error:e instanceof Error?e.message:"invalid request"},{status:400})}}
