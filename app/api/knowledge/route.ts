import {NextResponse} from "next/server";import {linkKnowledge,listKnowledge,searchKnowledge,updateKnowledge,upsertKnowledge} from "../../../lib/knowledge";
export const runtime="nodejs";export const dynamic="force-dynamic";
export async function GET(request:Request){const q=new URL(request.url).searchParams.get("q");return NextResponse.json(q?{records:searchKnowledge(q)}:listKnowledge(),{headers:{"Cache-Control":"no-store"}})}
export async function POST(request:Request){try{const b=await request.json();if(b.action==="upsert")return NextResponse.json(upsertKnowledge(b.record),{status:201});if(b.action==="update")return NextResponse.json(updateKnowledge(String(b.id),b.patch));if(b.action==="link")return NextResponse.json(linkKnowledge(String(b.from),String(b.to),b.relation),{status:201});return NextResponse.json({error:"Unsupported knowledge action"},{status:400})}catch(e){return NextResponse.json({error:e instanceof Error?e.message:"knowledge error"},{status:400})}}