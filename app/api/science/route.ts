import {NextResponse} from "next/server";
import {addEvidence,createDecision,createExperiment,createObjective,listScience,runExperiment,updateExperiment} from "@/lib/science";
export async function GET(){return NextResponse.json(listScience(),{headers:{"Cache-Control":"no-store"}})}
export async function POST(req:Request){try{const b=await req.json();if(b.action==="objective")return NextResponse.json({objective:createObjective(b.value)},{status:201});if(b.action==="experiment")return NextResponse.json({experiment:createExperiment(b.value)},{status:201});if(b.action==="experiment.update")return NextResponse.json({experiment:updateExperiment(b.id,b.patch)});if(b.action==="experiment.run"){return NextResponse.json({run:await runExperiment(b.id,b.kind,b.sandboxId,b.argv)},{status:201})}if(b.action==="evidence")return NextResponse.json({evidence:addEvidence(b.value)},{status:201});if(b.action==="decision")return NextResponse.json({decision:createDecision(b.value)},{status:201});return NextResponse.json({error:"unknown action"},{status:400})}catch(e){return NextResponse.json({error:e instanceof Error?e.message:"invalid request"},{status:400})}}
